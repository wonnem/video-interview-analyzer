# 1. 파이썬 실행 시 자동으로 생기는 임시 찌꺼기 파일들 무시
__pycache__/
*.pyc

# 2. 용량이 너무 큰 데이터 폴더나 결과물 이미지 폴더 무시
data/raw/
results/figures/

# 3. AI 관련 개인 설정 폴더 무시 (.omc 같은 파일)
.omc/

# 4. 내 가상환경 폴더 무시 (설치된 라이브러리 뭉치들)
venv/
.venv/

# 5. 민감정보 환경변수 파일 (절대 GitHub에 올리면 안 됨)
.env

# 6. 업로드된 임시 영상/오디오 파일
temp/

# 7. 테스트용 영상 파일 (용량 크고 불필요)
tset-video/
test-video/

# 8. 마스킹된 신분증 이미지 (민감정보, 서버에만 보관)
masked/

# 9. Claude 참조용 지시사항 파일 (로컬 전용)
DIRECTIVES.md